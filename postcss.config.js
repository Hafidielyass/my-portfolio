// Tailwind CSS v4: use the unified plugin from @tailwindcss/postcss
// This replaces the older separate tailwindcss + autoprefixer entries.
module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
